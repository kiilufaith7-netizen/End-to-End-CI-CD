{
  "name": "myapp",
  "version": "1.0.0",
  "description": "Simple Node.js app for ECS Lab",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo 'Running unit tests...' && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
